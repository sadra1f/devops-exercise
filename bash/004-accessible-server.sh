#!/bin/bash

# Write a program that has the IP of a server and its User/Pass in front of
# the Script name and if it is pingable, sends its /etc/passwd file
# to /home/user path of that server, otherwise a message displayed that
# the server is not accessible.

echo "Not implemented"
