#!/bin/bash

# Write a program that reads, pings one by one from within a file containing
# the list of destination IPs, and saves the result in a log file on the same
# day with the hostname of that machine.

echo "Not implemented"
